galaxy_info:
  role_name: docker_cleanup
  author: Alex Klinkert
  description: Role to cleanup unused docker resources every night.
  license: Apache v2.0
  min_ansible_version: 2.5
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all
  galaxy_tags:
    - docker
    - docker cleanup
    - systemd
    - systemd service
    - docker compose stack
    - prune
    - cleanup
dependencies: []
